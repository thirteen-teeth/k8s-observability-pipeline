---
apiVersion: "clickhouse-keeper.altinity.com/v1"
kind: "ClickHouseKeeperInstallation"
metadata:
  name: chk
  namespace: olap
spec:
  configuration:
    clusters:
      - name: "simple-3"
        layout:
          replicasCount: 3
    settings:
      logger/level: "information"
      logger/console: "true"
      listen_host: "0.0.0.0"
      keeper_server/storage_path: /var/lib/clickhouse-keeper
      # Raft logs and snapshots go to dedicated volumes (see volumeMounts +
      # volumeClaimTemplates below) so coordination I/O is isolated from data.
      keeper_server/log_storage_path: /var/lib/clickhouse-keeper/logs
      keeper_server/snapshot_storage_path: /var/lib/clickhouse-keeper/snapshots
      keeper_server/tcp_port: "2181"
      keeper_server/four_letter_word_white_list: "*"
      keeper_server/coordination_settings/raft_logs_level: "information"
      keeper_server/raft_configuration/server/port: "9444"
      prometheus/endpoint: "/metrics"
      prometheus/port: "7000"
      prometheus/metrics: "true"
      prometheus/events: "true"
      prometheus/asynchronous_metrics: "true"
      prometheus/status_info: "false"
  templates:
    podTemplates:
      - name: default
        spec:
          # Spread keeper replicas across nodes. whenUnsatisfiable is set per
          # environment via the cluster-vars ConfigMap (Flux postBuild):
          # local (single-node) uses ScheduleAnyway so all 3 replicas can
          # co-schedule; dev/prod use DoNotSchedule to force one per node.
          topologySpreadConstraints:
            - maxSkew: 1
              topologyKey: "kubernetes.io/hostname"
              whenUnsatisfiable: "${keeper_spread_policy:=DoNotSchedule}"
              labelSelector:
                matchLabels:
                  app: clickhouse-keeper
          containers:
            - name: clickhouse-keeper
              imagePullPolicy: IfNotPresent
              image: "clickhouse/clickhouse-keeper:26.3-alpine"
              # Mount each volumeClaimTemplate so the operator actually
              # provisions the PVCs; an unreferenced VCT is never created.
              volumeMounts:
                - name: default
                  mountPath: /var/lib/clickhouse-keeper
                - name: snapshot-storage-path
                  mountPath: /var/lib/clickhouse-keeper/snapshots
                - name: log-storage-path
                  mountPath: /var/lib/clickhouse-keeper/logs
              resources:
                requests:
                  memory: "256M"
                  cpu: "1"
                limits:
                  memory: "4Gi"
                  cpu: "2"
    volumeClaimTemplates:
      - name: default
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: "${keeper_data_size:=10Gi}"
      - name: snapshot-storage-path
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: "${keeper_snapshot_size:=10Gi}"
      - name: log-storage-path
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: "${keeper_log_size:=1Gi}"
---
apiVersion: v1
kind: Service
metadata:
  # DNS would be like zookeeper.namespace.svc
  name: zookeeper
  namespace: olap
  labels:
    app: zookeeper
spec:
  ports:
    - port: 2181
      name: client
    - port: 7000
      name: prometheus
  selector:
    app: clickhouse-keeper
    what: node
